admin:
  email: admin@email.com
  encrypted_password: <%= Devise::Encryptor.digest(User, 'password') %>
test:
  email: test@email.com
  encrypted_password: <%= Devise::Encryptor.digest(User, 'password') %>
one_fam:
  email: one_fam@email.com
  encrypted_password: <%= Devise::Encryptor.digest(User, 'password') %>
