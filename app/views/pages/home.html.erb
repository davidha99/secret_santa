<main class="container">
  <h1>Secret Santas Generator</h1>
  <p>A blazing fast Secret Santa Generator</p>

  <% if user_signed_in? %>
    <%= link_to "My profile", families_path, class: "btn btn--dark" %>
  <% else %>
    <%= link_to "Sign in", new_user_session_path, class: "btn btn--dark" %>
  <% end %>
</main>
