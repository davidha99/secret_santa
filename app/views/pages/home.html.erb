<div class="container">
  <div class="welcome">
      <h1>Secret Santa</h1>
      <p>A blazingly fast Secret Santa Generator</p>

      <% if user_signed_in? %>
        <%= link_to "Go to My Fam", families_path, class: "btn btn--dark" %>
      <% else %>
        <%= link_to "Sign in", new_user_session_path, class: "btn btn--dark" %>
      <% end %>
    </div>
</div>
