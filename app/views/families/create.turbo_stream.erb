<%# <%= turbo_stream.prepend "families", partial: "families/family", locals: { family: @family } %>
<%# same thing as below line %>
<%= turbo_stream.prepend "families", @family %>
<%= turbo_stream.update Family.new, "" %>
<%= render_turbo_stream_flash_messages %>
